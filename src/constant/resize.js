export const CANVAS_RESIZE = {
    // 히트맵에서 이 색(정확히 일치)으로 픽셀을 칠합니다.
    COLOR: [250, 0, 250], // Magenta
    // 보이는 핸들(우하단)에 그릴 정사각형 크기
    KNOB: 14,
    // 히트 박스 여유(픽셀)
    HIT_PAD: 18,
    // 최소 사이즈
    MIN_W: 100,
    MIN_H: 100,
};
